[pytest]
asyncio_mode = auto
norecursedirs = integrace

markers =
    real_llm: tests that require real LLM API (Gemini) - cost money!
    slow: tests that take significant time to run
    integration: integration tests requiring full system
