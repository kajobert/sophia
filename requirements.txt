# Sophia Core Dependencies
# This file lists only the direct, top-level dependencies of the project.
# Sub-dependencies are intentionally omitted. Install these into a clean
# virtual environment, and then run `pip freeze > requirements.txt` to
# generate a new, stable, fully-pinned list of all packages.

crewai
google-generativeai
python-dotenv
fastapi
uvicorn
psutil
pyyaml
memorisdk
langgraph
pyautogen
pytest
torch
