#!/bin/bash
echo "🔍 SOPHIA Status"
echo "================"
if pgrep -f "python.*run.py" > /dev/null; then
    PID=$(pgrep -f "python.*run.py")
    echo "✅ Běží (PID: $PID)"
    echo "💾 Paměť: $(ps -p $PID -o rss= | awk '{print $1/1024 " MB"}')"
    if curl -s http://127.0.0.1:8000/api/stats > /dev/null 2>&1; then
        echo "✅ API odpovídá"
        curl -s http://127.0.0.1:8000/api/stats | python3 -m json.tool
    fi
else
    echo "❌ SOPHIA neběží"
fi
